# qdd.app 全站内容页审查与改造记录

## 问题判定

用户截图中的日期条、申请入口、官方链接、资料清单和纠错区都退回了浏览器默认排版。根因不是单个模块的 CSS，而是本地 `file://` 预览时，内容页引用 `/assets/styles.css`、`/assets/app.js` 等根绝对路径；浏览器会把它们解析到系统卷根目录，资源加载失败。

线上 `https://qdd.app` 的根路径引用可以工作，但这造成了“线上正常、本地打开失效”的双重体验。

## 专家小组结论

### 前端结构审查

- 内容页资源路径按页面深度改为相对路径：首页 `assets/`，一级内容页 `../assets/`，国家/专题详情页 `../../assets/`。
- `check.mjs` 新增 CSS/JS 资源存在性检查；50 个 HTML 必须能从自身目录解析到真实资源。
- 完整申请面板移到国家页教程之前，顺序调整为：首屏结论 → 政策状态 → 快速入口 → 申请/入境面板 → 分步教程 → 政策字段 → 官方来源。

### 视觉与交互审查

- 申请入口、材料、官方来源必须是明确的块级组件，不能让一串长链接连成浏览器默认文本。
- 资料卡使用“核对 / 按需”，不把结构化参考清单伪装成每个人固定强制提交。
- 移动端 320–390px 的申请按钮改为单列全宽，长标题允许任意位置换行，避免裁切和横向滚动。
- 更新台、专题页、方法论页的目的地列表、方法卡、更新记录统一为可读的网格/列表组件。

### 信任与政策表达审查

- “待复核”状态现在位于国家页首屏行动入口之前；待复核页主按钮显示“先核对官方要求”，并注明材料层核验日期不等于政策层已复核。
- 抽象的“高/中高风险”改为“政策变动风险 / 入境条件 / 材料核验”等可理解提示，不暗示个人拒签概率。
- 免签页不再显示签证成功率模块，改为“入境决定口径”；申请签证页保留官方统计，但同屏显示“不可换算个人成功率”。
- 免签页不显示费用/支付占位链接；重复指向同一官方 URL 的越南入口会去重。
- 俄罗斯入口明确为“中国驻俄使馆｜免签说明”；电子签仅作为允许用途核对路径，工作、学习、居留仍需相应签证。

## 发布闸门

```bash
node visa-cards/generate-visa-cards.mjs
cd qdd-site
npm run build
npm run check
```

通过标准：50 个 HTML、20 个国家、172 节教程；内部链接、元数据、脚本、署名以及每页 CSS/JS 相对资源均通过。在线部署后另抽查首页、日本/美国/泰国/俄罗斯三种政策类型和本地 `file://` 国家页视觉资源解析。
